[project]
name = "boba-cli"
version = "1.0.0"
description = "Interactive CLI for Boba Vending Machine FSA Simulator"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.5",
    "rich>=14.2.0",
]

[project.scripts]
boba = "src.boba_cli.main:main"
